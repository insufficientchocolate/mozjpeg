@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/@TARGETS_EXPORT_NAME@.cmake")

if(BUILD_SHARED_LIBS)
  set(MOZJPEG_LIBRARIES MOZJPEG::jpeg)
else()
  set(MOZJPEG_LIBRARIES MOZJPEG::jpeg-static)
endif()

check_required_components(@CMAKE_PROJECT_NAME@)
